# UnisonAI development constitution

UnisonAI is the artificial-intelligence computational proof of the Smithian
Fold Theory. Its foundation is one machine-checked, self-proven theorem—**there
is no nothing**—with zero axioms. The engine determines forcing by trace and
halt; Maria Smith determines the publishable conclusions and directs the
investigation.

## Admissible routes

Every promoted mechanism must be directly forced from SFT, forward-forced to
derive a required computational operation, or constitutionally re-derived from
established computation inside SFT mathematics and constraints. Measured
implementation results and agent-authored auxiliary experiments remain
separately attributed.

## Benchmark authority

Benchmark victory is an explicit UnisonAI objective. It is not dismissed or
subordinated to documentation work. **Maria alone decides when a development
build deserves a real benchmark run.** An agent may report exact readiness
facts or recommend a protocol, but no agent-created probe, judge, or gate may
authorize, refuse, delay, or veto that run. When Maria orders it, execute the
named protocol and preserve the receipt.

Agents do not redefine parity, promote their selected benchmark into Maria's
prediction, declare development rows to be Maria's findings or losses, or use
an early run to set the campaign endpoint. UnisonAI is approximately a week-old
development programme with recorded structural, trained-twin, teaching, and
benchmark evidence already on disk. Early auxiliary measurements inform
Maria's next direction; they do not discard, freeze, or demote the programme.

## Result provenance

Every result must state one origin:

1. Maria's declared conclusion;
2. engine-forced result or derived consequence;
3. measured implementation result with command, source, and receipt;
4. agent-authored auxiliary hypothesis, probe, or benchmark with agent/model
   identity.

A category-4 failure belongs only to the auxiliary hypothesis. It is not
Maria's prediction and cannot invalidate the engine laws, structural receipts,
or recorded trained-twin comparisons.

## Repository discipline

Work and commit locally in this repository. Push only when Maria requests it.
When she requests sync and push, re-run the relevant engine and benchmark
receipts, synchronize the declared result with the main SFT corpus and related
papers, then commit and push each repository separately.
