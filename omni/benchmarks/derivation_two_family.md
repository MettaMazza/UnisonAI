# The two-family question — derivation target

**Status: CLOSED — forced in the corpus.** The derivation lives as
ErnosPlain steps in the theory repository, verified by `ernos` with only
`ok` lines, documented as Steps 308–313 of OneFoldMaster.md:
`constants/two_family_harmonics.ep` and `constants/two_family_selection.ep`
(this note's question), plus the follow-on forced laws
(`deposition_law.ep`, `functional_band.ep`, `hold_closure_regimes.ep`,
`partition_localization.ep`). Exact whole numbers and fractions
only, every claim under `forced_to_be`/`forced_unique`, domain-guarded,
the atlas behind the Measured wall. Forced: the generator pair {2, 3}
(two independent readings agree), the family count = the generator count
= two, the colour family's exact quantities {1/4, 3/4} — the same forced
pair the spine holds as the electroweak mixing (Step 10, reused not
re-derived) — and family distinctness. Not forced, stated: the selection
law, and the class identification at the Measured wall.

**The measured fact (committed: `basis_atlas_results.txt`,
`basis_atlas_round2_results.txt`):** the law-bearing concentration of
trained weights appears in two spectral families — dyadic (Walsh) and
smooth (DCT) — with the slant arm showing no continuum between them. The
token embedding carries the law in every model probed (11/11); the family
lean varies by training lineage (Llama/Pythia dyadic-leaning, the rest
smooth-leaning); attention and contraction classes read quiet everywhere.

**What is already forced.** The Walsh functions are the characters of the
binary digit group, and the fold map x → 2x mod 1 is the shift on it: the
dyadic family is the fold map's own harmonic family, by theorem. Its
signature — concentration exactly invariant under F2-linear reindexing —
is the standing self-test of every probe in this directory and holds on
every recorded run.

**The derivation target, for the corpus.** From the One and its two
generators, force: (1) the identity of the second family; (2) why exactly
two appear and no third; (3) what property of a training recipe selects
the family its embedding writes in. The derivation precedes any further
instrument: an instrument's reading only means something once the forced
claim fixes what it would verify.

**The corpus's own ingredients (OneFoldMaster.md, cited — assembled for
the authoring of the forcing step; no conclusion is drawn here):**

- **Step 4 — the two generators, counted.** `b = 2` and `c = 3` are the
  two smallest fold periods beyond the One, read off the fold's period
  spectrum (periods of 1/3 and 1/7), chosen by nothing. The enforcement
  layer (`forced_to_be`, `forced_unique`, `forbid_selection`,
  `require_in_domain`) is the standard any new step must meet. Note: in
  the spine, 3 is a **counted period**, not an operator — any derivation
  treating "multiplication by 3" as the colour generator's action imports
  a construct the spine does not supply.
- **Step 18 — three of everything.** The corpus's own tripling construct:
  the **tripling fold's fibre** over 2/3 (size 3, cross-checked = colour),
  and the uniqueness form that may bear on "no third family": the
  stability window `(binary, binary²) = (2, 4)` contains **exactly one**
  integer.
- **Step 10 / the W–Z step — the corpus's lawful cosine family.**
  Rotational/mixing structure enters the theory as **exact cosine-squared
  quantities forced from the generators** (`cos²θ_W = 1 − 1/b² = 3/4`),
  with the irrational ratio handled by comparing the **square** — the
  domain-lawful form any smooth-family statement must take.
- **The dyadic side.** The fold **is** doubling; its harmonic family is
  the engine's integer Walsh eye (exact integer Parseval, in the UnisonAI
  wing) — the only family the record holds as the fold's own by
  construction.
- **The Measured wall (Step 4's `forbid_target_input`).** The atlas
  margins are `Measured`-type quantities: they may sit only on the
  checking side of a forced claim, never inside its derivation. Any
  derivation of the two-family question must run entirely on the exact
  side and meet the atlas afterward, as every constant in the spine meets
  its measurement.
