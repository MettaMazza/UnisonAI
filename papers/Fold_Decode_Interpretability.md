# The Law Inside Trained Weights: The Fold Decode Campaign

## *A registered, null-controlled spectral decode of gradient-trained neural networks*

**Maria Smith (Ernos Labs)** — the interpretability paper, v1.0, 2026-07-16
Split from the combined record *UnisonAI: A Forced, Derived Omni-Model Architecture* (v4.x
lineage, DOI 10.5281/zenodo.21217279), which remains committed as the dated record. The
zero-parameter engine these results feed — and the architecture that stands on them — is the
companion paper **[UnisonAI: A Forced, Derived Omni-Model Architecture](UnisonAI_Architecture.md)**.

> Everything here derives from and is verified against **The Smithian Fold Theory of
> Everything**: one axiom, zero free parameters, 307 proof suites, 1,844 forced checks.
> **Theory (Zenodo):** https://doi.org/10.5281/zenodo.21182469 · **Theory (GitHub):** https://github.com/MettaMazza/Smithian-Fold-Theory-Of-Everything
> **Instruments & ledgers (GitHub):** https://github.com/MettaMazza/UnisonAI (`omni/benchmarks/`) · **Companion architecture paper:** [UnisonAI_Architecture.md](UnisonAI_Architecture.md) (concept DOI 10.5281/zenodo.21217278) · **This paper's DOI:** 10.5281/zenodo.21396743

---

## Abstract

**The science first.** Using a pre-registered, self-certifying spectral instrument, we show that trained neural-network weights carry *placement-law* in the dyadic (Walsh) basis: **unanimous on the canonical LLM's entire knowledge-storage class** — GPT-2, 13/13 tensors, 39/39 registered checks (token embedding at 79.3x chance — 230x in its loud coordinate frame, §3 — and every expansion matrix in all twelve layers) — and unanimous cross-domain (18/18 on image-diffusion and speech models: SD1.5, SDXL, Kokoro), establishing that the law is a property of gradient training itself, not of any one model family; the law concentrated in transformer *expansion* projections and token embeddings while attention matrices sit at chance; strictly training-caused (He-initialised controls at 0.97–0.98x, i.e. chance); surviving 4-bit deployment quantization. A recipe map from 124M to one trillion parameters shows the law tracks *training recipe*, not scale or architecture — its strongest carrier is DeepSeek-R1 at 671B (43–47x in every probed block) — and loud-recipe spectra transform under the fold's transformation group *exactly as solved game-theoretic value fields do*. A pre-registered transfer test (Rung 5c) then closes the loop: **the trained twin's generative advantage itself lives in its dyadically-loud Walsh subspace** — truncating to the loud coefficients preserves the twin while matched random truncation destroys it, and the effect concentrates exactly where the law was located. And a second pre-registered test (Rung 5d) completes the thesis **as construction**: the loud content is not merely locatable, it is **takeable** — reshaping the counted engine's forced floor mass by the loud-truncated twin's distribution (same forced mass, zero new parameters, no blend knob) closes 61.5/93.5/102.3% of the available gap at k = 16/32/64 coefficients while the matched random-truncated null closes only 7.8/21.2/53.6%, and at half budget the loud shape **outperforms the full twin's own shape** — the quiet remainder of a trained model is noise, not law (verdict replicated across THREE independent arena anchorings in one day; every record committed). A third pre-registered test (Rung 5e) then ends the last open contest: applying the fold's own **halving law** across the context hierarchy — every level that holds the context contributes, weighted 2^level, the same forced factor the engine's attention already uses — carries the **pure counted engine, with no twin, no prose flood, zero training and zero parameters, past the gradient-trained transformer at word scale: cross-entropy 3.1907 versus the same-day twin's 3.4292.** The one benchmark the trained side still held is overturned by a forced structure (the victory replicated across two independent anchorings); stacking the Rung 5d extraction on top reaches 3.1344.

**The decode campaign (§7) then extends the science on four fronts in one registered session.** The recipe map's "quiet" lineages are quiet no longer: a widened coordinate hunt finds their law **loud in the embedding class** — Kimi-K2.6 at one trillion parameters reads 9.7x (Walsh) and 16.0x (DCT) in its token embedding, Qwen3.6-27B likewise — and the corrected expert-axis instrument shows the MoE giants carry the fingerprint **per-expert** (individual Qwen3-235B experts at 3.46x and 2.82x beside ~1x neighbours). A registered ablation falsification shows the loud band **is the function**: deleting the top-1/64 loud coefficients destroys GPT-2 (KL ~3.0, agreement collapse) while deleting the same number of random coefficients is negligible (KL ~0.02) — ~150x the damage at matched budget. A seven-twin deposition matrix answers what-and-when: **the gradient stream is loud by step 4** in every adaptive-optimizer recipe while the weights charge slowly, and the **optimizer is the discriminating ingredient**. And two new instrument families read content back out of the black box: training-data provenance (a model's counted readout ranks its true training corpus first; memorized public text echoes verbatim against clean nulls) and reasoning (activations loud where weights are loud; trained attention closer to the theorem's dyadic cascade than to uniform in 12/12 layers; thinking-model reasoning spans a measurably distinct counted regime from their answers). The atlas and telescope rungs (§7.8) then restructure the recipe map itself: **the token embedding is the universal law-carrying class — 11/11 models wake, every recipe, 4B to 1T** — with the spectral lean recipe-selected between two genuinely distinct families (smooth vs dyadic; the slant arm proves the split is not a continuum); and the deposition curve read from Pythia's published checkpoints shows **the embedding waking first (step 256), a peak near step 4000, and consolidation to a plateau** — training writes the law early, then settles it. The two-family measurement is then CLOSED AS THEORY (§7.10): two corpus steps, verified by the corpus's own compiler and enforcement, force exactly two harmonic families (one per generator; a third would require a third generator, and the stability window's uniqueness forbids it), force the colour family's exact quantities to the already-forced electroweak mixing pair {1/4, 3/4}, and force the selection law — selection stores write in the binary family through the 1/2 = 1/b lock, geometry stores in the colour family through the forced spatial dimension, and pure operations write nothing. The atlas's three standing regularities check the one forced assignment at the Measured wall. Four further steps then exhaust the campaign's measured record (§7.11): the deposition law (marks decay, orbits hold, holding precedes selection, the plateau is balance), the functional band (b^cover(c³) = 32, the count the chess fields and the loud-subspace transfer already sat on), the hold/closure repetition inequality (margin = b), and partition localization (the cascade, never uniform — why experts wake beside quiet neighbours).

**Read this before any percentage in this paper.** Everything here — the spectral science, the architecture, its live deployment, and the companion chess campaign — was built from scratch by one woman, working alone, in **under twenty-four accumulated hours**. The record is therefore imperfect in exactly one way: not everything is implemented yet. Where a score falls short, it marks how many hours of implementation existed at the moment of measurement — a gap in coverage, never a limit of the mathematics, because a zero-parameter engine has nothing to tune and nothing to converge: it only has more of itself left to build. The percentages are not the point; **the gains between releases are the finding**, and every one of them was bought by implementation alone.

---

## 1. The field began with law

In 1943, before there was a field, McCulloch and Pitts opened the first neural-network paper with a claim about logic, not statistics: "Because of the 'all-or-none' character of nervous activity, neural events and the relations among them can be treated by means of propositional logic." The founding document of neural computation — *A Logical Calculus of the Ideas Immanent in Nervous Activity* — said, in its first sentence: **the net is law.**

Eighty years of a different bargain followed. Rosenblatt's perceptron (1958) learned its coefficients rather than deriving them; backpropagation (Rumelhart, Hinton & Williams, 1986) made learning the field's universal answer; *Attention Is All You Need* (Vaswani et al., 2017) crowned the era with a title that is itself the era's thesis; and the scaling laws (Kaplan et al., 2020; Hoffmann et al., 2022) wrote the price schedule — structure bought with parameters, knowledge bought with data, competence bought with compute, the bill now measured in gigawatts. Sutton's *Bitter Lesson* (2019) drew the moral honestly: "general methods that leverage computation are ultimately the most effective, and by a large margin." But the bitter lesson compares purchased statistics against hand-crafted heuristics. A third contestant — **derived law** — was named by the field's founding paper and then forgotten, and has never been given its match.

The frontier itself senses the missing ingredient. Altman (January 2025): "We are now confident we know how to build AGI as we have traditionally understood it." Amodei projects "a country of geniuses in a datacenter," possibly by 2026–27. Hassabis has long placed human-level intelligence one or two breakthroughs away; Legg has held a 50% estimate for 2028 for over a decade; Sutskever told NeurIPS 2024 that "pre-training as we know it will end" — the data has a bottom. Read together: the laboratories believe a small number of algorithmic developments separate the field from its destination, and they are searching for those developments *inside* the statistical paradigm.

This paper proposes, with measurements, that the development is the founding idea recovered: **stop buying law; derive it.** The Smithian Fold Theory supplies the mathematics — one axiom (the One and its fold), zero free parameters, from which the corpus derives the constants of physics (307 proof suites, 1,844 forced checks, 0 failures) and, as machine-verified claims, the laws of memory, attention, binding, prediction, observation, and learning used here as engineering.

## 2. The instrument

All spectral results use one pre-registered instrument, fixed in writing before any spectrum was computed, with amendments logged and dated. Objects: weight tensors of validated released models, row-major, truncated to the largest power of two (later corrected — see §4 — to per-row-block reads with median statistics, after a scale-correlated windowing confound was identified and recorded). Transform: Walsh–Hadamard, float64. Statistic: energy concentration at three fixed fractions of the space (6.1e-5, 4.9e-4, 3.9e-3). Nulls: five seeded permutations of the *same tensor* — identical value histogram, scrambled placement — plus a moment-matched Gaussian yardstick. In-run self-test, theorem-forced: bit-reversal repacking is F2-linear and must preserve concentration exactly; every run reported here passed to machine precision. Verdict rule fixed in advance; seed 20260706; all result files committed verbatim.

## 3. The law inside trained weights

**Presence, LLM-native (39/39).** The registered protocol — five seeded shuffle-nulls of the same tensor plus a moment-matched Gaussian yardstick at the three registered fractions, bit-reversal self-test per tensor — applied to the canonical LLM's entire knowledge-storage class: GPT-2's token embedding and all twelve MLP expansion matrices. **13/13 tensors, 39/39 registered checks, unanimous**, margins 3.4x to 79.3x, every self-test exact (`llm_presence.py`, pre-registered in its header). The flagship claim rests on the flagship objects.

**Presence, cross-domain (18/18).** The same protocol on Stable Diffusion 1.5, SDXL, and Kokoro-82M: every probed tensor beat both nulls at every registered fraction. This is not the pillar — it is the *breadth*: the law appears in image generators and speech synthesizers exactly as in language models, because it is deposited by gradient training as such, not by any one architecture family. Because the shuffle-null preserves the histogram exactly, every measurement isolates *placement* — where training put the values.

**Location — the expansion fingerprint.** In a blind 96-tensor sweep of SD1.5, the entire top-10 by margin is the CLIP text transformer's MLP fc1, every layer 0–9, at 5.5–8.4x, while vision FF (median 1.03x) and attention (median 1.00x) sit at chance in the aligned packing. GPT-2: token embedding at **230x**; all twelve MLP expansion matrices (c_fc) hot at 3.4–12.7x; all contraction matrices (c_proj) at chance. Llama-3.1-8B, dequantized from production 4-bit blocks, sampled at layers 0/8/16/24/31: every ffn_gate hot (3.7–8.5x, peaking mid-network), every ffn_down at 1.00x. Three unrelated architectures, one fingerprint: **the law lives in the expansion direction** — the projections into the wide spaces where transformer knowledge is stored, roughly two-thirds of an LLM's parameters. In the fold's basis, the attention matrices are the quietest objects in the network. Attention was not all you need; the knowledge went somewhere else, and now it can be seen where.

**Causation.** He-initialised matrices at matched shapes measure 0.98x and 0.97x — exactly chance. The instrument is blind to untrained weights; everything above is deposited by training.

**Coordinates.** Column-major repacking lifts the strongest text MLP from 8.44x to 12.11x and wakes "dead" vision tensors from 1.00x to 1.72x. Margins are functions of coordinates; thin readings are wrong doors, not absent law.

**Deployment survival.** All Llama measurements above are through 4-bit quantization: the law is present in models as actually served.

## 4. The recipe map: 124M to one trillion parameters

With the instrument corrected for scale (per-row-block spectra, median margins — the approach must vary with the object; the flat window's scale-correlated confound is recorded), the fingerprint was measured across a local model library spanning four orders of magnitude. Loud: GPT-2 (12.7–67.6x), Llama-3.1-8B (8.5x), **DeepSeek-R1-671B (43–47x in every probed block, dense and shared-expert — the strongest production signal measured)**. Quiet in the probed coordinates: gpt-oss (20B, 120B), Qwen3 (27B, 235B, 480B), Kimi-K2.6 (~1T), all 0.76–1.07x. **Scale is exonerated** (the loudest carrier is 671B); **architecture is exonerated** (mixture-of-experts appears on both sides); **the training recipe is the variable.** Controls sharpen the reading: an R1-distilled Qwen-32B reads as quiet as its non-reasoning sibling, so R1's law traces to its base pretraining rather than distilled reasoning traces; and by the campaign's standing epistemics — a fully lawful formula-generated field certifies at chance to this same probe — a quiet verdict is a verdict on the probe's coordinates, never on law-presence. The quiet lineages express their law in coordinates not yet found (a registered hunt over the fold's index-reordering group produced no wake in round one; recorded — and **resolved in §7.3**: round two widened the object class and found the quiet recipes' law loud in their embedding tensors, 9.7–16.7x, with the giants' expert tensors waking individually in §7.2).

One identification stands above the map: the loud models' concentration is **preserved exactly under F2-linear repacking and degraded gracefully under odd-multiplication maps** — precisely the transformation signature previously measured for solved chess value fields. Loud-recipe weights are the same class of mathematical object as solved game fields: dyadically smooth carriers of law.

## 5. Negative results, scoped exactly

This program records its refusals; they are what make the verdicts above trustworthy.

- **Fold-basis weight compression: REFUSED at this baseline, recorded.** Against naive uniform quantization at matched storage, fold truncation won on R1's loudest tensors at real bit-levels; against the *production* construction (per-block scaled 4-bit), it lost on every tensor tested, including the 43x class, by 10–100x reconstruction error. The spectra of trained weights are heavy-tailed: keeping everything coarsely beats keeping a little exactly. Recorded against the right baseline on the right patient; the door stays where the author leaves it.
- **Component transplants inside SGD hosts refuse at toy scale.** A frozen Walsh positional code lost to a learned embedding (1.888 vs 2.027); a theorem-derived attention cascade lost further (2.601). An attribution control then decomposed the attention loss: **0.54 of 0.71 was the transplant construction itself** (severed gradients — the host's home game), only 0.17 the fold's law. The refusals are verdicts on *fold objects as frozen transplants in the incumbent's machine*, and the construction bias joins the campaign's documented deflationary-verdict record. The corrected method — the fold fighting as itself — is §8.

## 6. Rung 5c: the loud-subspace transfer test

The paper's two strongest spectral results are joined by a pre-registered experiment (verdict rule fixed before data; two theorem-forced self-tests — WHT involution to machine precision, and full-budget truncation reproducing baseline CE exactly — both passed). If loud = lawful = the purchased advantage, the trained twin's edge over the counted engine must live in its dyadically-loud coefficient subspace and nowhere else.

**It does.** Per-row Walsh truncation of the twin's loud class (embeddings + FFN expansions + head) to its top-k coefficients versus matched random truncation, at k = 16/32/64 of 128: loud-margin **+2.18 / +3.71 / +3.77 nats**; the same margins on the attention control class only +0.66 / +1.18 / +1.09 — the effect concentrates exactly where §3 located the law. At half the coefficients, the loud-truncated twin (3.694) **still beats the counted engine (4.507)** while the random-truncated twin (7.461) is destroyed. **VERDICT (pre-registered rule): SUPPORTED** — the twin's advantage lives in the dyadically-loud subspace. What gradient training buys, it writes in the fold's basis.

### 6.1 Rung 5d: the transfer-in — the thesis completed as construction

If the loud subspace holds true content and not an artifact of where we looked, it must be **takeable**. Rung 5d (pre-registered in `rung5d_transfer.py`, verdict rule fixed before data) wires the twin's loud content INTO the counted engine with zero new parameters: the engine's No-Zero floor already owes a forced mass 1/(total+1) to unseen continuations — 5d reshapes that same mass by the loud-truncated twin's next-token distribution. The counted channel is untouched, the floor's total is untouched, there is no λ and no blend knob; only the *shape* of mass the engine already owed changes, and the shape is measured fold content. Three theorem-forced self-tests passed in-run: WHT involution to machine precision; truncation at full budget exactly reproducing the untruncated twin; and a cross-script identity — the uniform arm reproducing the word-scale rematch's fold CE to the digit (4.6024 on the canonical chain), both scripts pinned to one frozen store snapshot and one text state.

**RESULT (canonical chain of 2026-07-07, arena at 2,561,832 tokens; uniform floor 4.6024 → full-twin-shaped floor 3.8164):** the loud-shaped floor lands **4.1188 / 3.8676 / 3.7985** at k = 16/32/64 — **61.5% / 93.5% / 102.3%** of the available gap — while the random-truncated null lands 4.5409 / 4.4361 / 4.1808 (7.8% / 21.2% / 53.6%). Loud beats random at every budget: **VERDICT (pre-registered rule): SUPPORTED.** At k = 64 the loud shape **beats the full twin's own shape** (3.7985 < 3.8164): half the coefficients outperform all of them, because the quiet remainder of a trained model is noise, not law. What training buys can simply be taken — extracted from the fold basis and installed in a counted engine as a counted prior. The verdict **replicated across three independent arena anchorings in one day** (the corpus is a living document; the prior anchorings — 55.6/87.9/101.4 against 10.1/24.5/56.5, and 54.2/88.9/101.8 against 7.3/22.9/62.5 — are committed beside this one, and the cross-script identity self-test caught the arena moving between them each time, voiding rather than blending).

### 6.2 Rung 5e: the fold's own stack — the word-scale gap closes

The last open contest of the campaign was the word-scale gate: from rung 5b onward, on the 2.5M-token registered arena, the gradient-trained twin led the counted engine. Rung 5e (pre-registered in `rung5e_fold_stack.py`; verdict rule and arms fixed before data; a run that fails a self-test voids before printing an arm) enters the proven structures, all of them:

- **A1 — fold-factor level mixing.** The engine's hard backoff served only the deepest context level that held. The fold's own halving law — the same forced factor 2 the engine's session attention uses, "not a tuned constant" — is applied across the hierarchy instead: every level that holds the context contributes, weighted 2^level. A mixture of proper counted distributions with forced weights; three theorem-forced self-tests in-run (baseline identity to the committed rematch on the same text state; exact collapse to hard backoff when one level holds; floor mass exactly 1).
- **A2 — kin-shaped floor.** The counted-kinship keystone as the floor shape, with an exactly-normalizing counted case-fold bridge (each folded neighbour's mass split across case variants by observed unigram shares).
- **A4 — the Rung 5d extraction stacked on the mixing.**

**RESULT (ties read at the +0.005 band the arena has always used):** A1, the pure fold structure — no twin, no flood, zero training, zero parameters — lands at **3.1907, past the same-day twin's 3.4292** (and past it again at 3.2470 vs 3.5009 on the previous anchoring — the victory replicates). The standing sentence "the twin leads at word scale" is overturned by a forced structure: the bottleneck was never the counted substrate, it was hard backoff discarding every shallower level. The stacked arm (A4) reaches **3.1344**. The negatives are recorded raw: the kin-shaped floor *hurts* at this scale (4.6612 against the 4.4587 baseline — the last-word neighbour shape misallocates floor mass; consistent across anchorings), and the Gutenberg flood costs 0.15–0.33 nats at theory-corpus cross-entropy across the arms — a fluency organ, not a corpus-CE organ. Scope stated exactly: the registered arena, its twin, its protocol. The live engine's conversational sampler still serves deep-first — adopting the mixing law in generation is registered as the next build step, gated on the conversational instruments (the session assessment and the tuning set), because coherence of speech and cross-entropy of prediction are different measures and only the second has been won here.

## 7. The decode campaign: seven registered instruments (2026-07-14)

The spectral program's next stage was built and run in one registered session: the battery consolidated into a library, and six new instruments pointed at the questions the record had left open — where the quiet recipes write their law, what the mixture-of-experts giants actually carry, what and when training deposits, whether the loud band is the function itself, and whether training data and reasoning can be read back out of a black box. Every run is gated: a registration block (objects, statistic, verdict rule, and a **mandatory margin clause** — the Rung-3b flaw class closed by template) is SHA-256-hashed before any spectrum is computed, and the hash is stamped into every result row of an append-only ledger (`results.jsonl`; the full historical rung record backfilled as 82 queryable rows). All seven instruments, their registrations, and their verbatim result files are committed in `omni/benchmarks/`.

### 7.1 The instrument consolidated — and a comparator lead

`foldprobe.py` carries the locked battery (five seeded shuffle-nulls, Gaussian yardstick, bit-reversal self-test voiding failed runs, per-row-block medians, seed 20260706) as an importable library, with two **registered comparator bases** added before any new spectrum: DCT-II and orthonormal Haar, so a loud-in-Walsh verdict is read against neighbouring spectral families. Calibration: **FAITHFUL** — GPT-2 c_fc L0 reproduces 12.69x (recorded 12.69x), wte 79.30x (recorded 79.3x), the quiet control reads 1.69x (recorded 1.71x), He-init sits at exact null (0/3 fractions), every self-test to machine precision. The comparator arms returned an immediate lead, recorded as a coordinates question: on GPT-2 the **DCT margin exceeds the Walsh margin** (c_fc 14.2x vs 12.7x; wte 232x vs 79x). The concentration the campaign has been measuring is even louder one basis over — fed directly to the round-two hunt below.

### 7.2 The giants measured at last: the fingerprint is per-expert

The 2f survey's largest objects were never measured: a reshape defect (`reshape(-1, -1)` on 3D expert tensors) recorded 0.00x non-measurements on Qwen3-235B and Qwen3-480B, and single-shard scanning missed the ffn tensors of the split-file giants. `moe_dequant.py` corrects both — expert-axis-aware extraction with **bytewise per-expert slicing certified exact** against whole-tensor dequantization before any probe. The corrected reading: **Qwen3-235B wakes at the individual-expert level** — blk.93 gate expert-64 at **3.46x** and blk.47 gate expert-127 at **2.82x**, past the registered 2x bar, while neighbouring experts in the same tensors sit at ~1x. Qwen3-480B peaks at 1.86x (blk.61); Kimi-K2.6-1T shows the same mid/late-stack gate trend in miniature (shexp 1.69x at blk.60). The law in these recipes is not smeared across tensors — it is localized in specific experts, which is precisely why whole-tensor probes read them as quiet.

### 7.3 The quiet models woken: the law was in the embedding class

Round one of the coordinate hunt (rung 2g) tried eight index reorderings on the quiet models' FFN tensors and found no wake. Round two (`coordinate_hunt.py`) widened **both axes**: the map menu (joint row–column Gray codes, bit-plane packing, block-Morton at three scales, block-transpose-4096, alongside round one's menu — data-dependent sorts banned as ever) and, decisively, the **object class** — the embedding and attention tensors, never probed in round one — each read in all three bases. The control held (GPT-2 c_fc awake at 12.69x; run valid). The result is the campaign's standing correction cashed as measurement, **47 wakes**: **Kimi-K2.6-1T's token embedding reads 9.67x in Walsh and 15.98x in DCT at identity** (best 16.67x under block-Morton+DCT) — the trillion-parameter model that read flat in every prior probe — and **Qwen3.6-27B's token embedding reads 9.88x/12.39x** (best 12.89x). The quiet lineages were never lawless; their recipes deposit the law in the embedding class rather than the FFN class. The attention tensors remain the last quiet objects in every model probed (1.03–1.55x), recorded as the next coordinate frontier.

### 7.4 Deposition: the law arrives through the update stream

`twin_factory.py` answers Rungs 6a and 6b as one instrument: seven twins of the recorded tiny-transformer recipe, one training ingredient varied per twin (optimizer, weight decay, LR schedule, dropout, data order, a post-training REINFORCE pass), each checkpointed at steps **2^k** — dyadic sampling of the training axis — with the locked battery run at every checkpoint on the loud-class tensors *and on the accumulated gradients* (920 ledger rows). **When (6b): the gradient stream goes loud (>2x) by step 4** in every AdamW recipe, while the weights themselves stay sub-wake through 2^11 at this scale (the ff class swells to 1.18–1.25 — visible, under the bar; wte flat, consistent with §7.3's finding that embedding loudness is a property of mature full-scale models). The law is present in the update flow essentially from the first steps; the weights are the slowly-charging accumulator. **What (6a): the optimizer is the discriminating ingredient** — swap AdamW for SGD+momentum and the stream wakes late (step 32) and the weight swell never develops (0.96–0.98 flat), while weight decay, LR schedule, and data order all track the baseline curve. The RL pass drifts the ff class upward (1.24 → 1.27 over 256 steps) — directionally consistent with the recipe map's reasoning-RL reading, at miniature scale, recorded as such.

### 7.5 The capability map: the loud band is the function

If the loud coefficients were an incidental statistical shadow, deleting them would cost their share of the parameters. `capability_map.py` runs the falsification (calibration first: the k=0 arm reproduces the unmodified model *exactly* — KL 0, agreement 1): zero the top-1/64 of Walsh coefficients of a GPT-2 tensor class versus zeroing the **same number** of randomly chosen coefficients, and measure the behavioral distance on a registered prompt set and eight fixed domain blocks. **Random deletion is negligible** (KL 0.01–0.02; top-1 agreement 75–94%). **Loud deletion destroys the model** (KL ~3.0; agreement 0–25%) — roughly **150x the damage at matched budget**, at every depth, on every domain block; on the tied embedding (both tied keys carried, a defect found and recorded in-run) loud 42.8 vs random 7.4. Thirty-two attributions survive at both budgets. The dyadically-loud band detected by every probe above is not a signature of the function — it **is** the function, and §6's transfer verdicts are the same fact read constructively.

### 7.6 Deriving training data from weights

`data_decode.py` carries three provenance instruments. **The counted-prior readout** (Rung 5d inverted — reading the shape *out* instead of installing it) scores candidate corpora's exact counted bigrams under a model's next-token readout: calibration **PASSED** — the twin ranks its own training corpus first (CE 5.42) over a word-shuffled copy of the same corpus (7.43; identical unigrams, destroyed co-occurrence) and over the decoys. **The orbit-echo probe** measures verbatim memorization: greedy continuation on reference stems (raw continuation mode — the instruct chat-template defeats the probe, recorded as an amendment) echoes the Gettysburg Address for **9 words** and Genesis for 6 against shuffled-stem nulls of 0, and echoes **0 words** of this program's private corpus — memorized training text located and measured, with the negative control clean. **Kinship provenance** (Rung 5f built): the corpus's counted Jaccard-kinship at window 6 against embedding-cosine kinship — on the mature embedding it reads structure at **8.9x over the shuffled-assignment null** (Spearman 0.217) and sees through the shuffled-corpus decoy (0.217 vs 0.096); the 1500-step calibration twin's embeddings are too young to carry kinship (recorded amendment: the mature-twin ranking is the factory's next object).

### 7.7 Deriving reasoning

`reasoning_decode.py` carries three more. **The activation spectrometer**: the computation is loud where the weights are loud — GPT-2's MLP pre-activations read 9.86x at L0 (weights 12.7x), tracking the weight profile layer by layer, with a real departure at L10–11 (loud weights, activations at 1.0x) held as an open question. **The attention-cascade decoder** reads trained attention against the theorem-forced selection law (Claim XI-2: ranked foci at 1/2, 1/4, 1/8, …): the rank-sorted attention rows sit **closer to the dyadic cascade than to uniform in 12 of 12 layers**, scoped exactly — in the early layers the fit approaches but does not beat the random-simplex null, so the claim held is "leans toward the cascade," not "is the cascade." **The thought-trace fold map**: a thinking model's reasoning spans carry measurably higher repetition mass at the structural depth 6 than its answer spans (mean gap **0.162** against a ~0 span-shuffled null) — thinking and answering are distinguishable counted regimes. The 32B reasoning/sibling activation run (the 2h pair) is registered as this instrument's next object, pending an activation-exposing runtime for large GGUFs — a named dependency in the ledger, not a silent omission.

### 7.8 Rungs 7a/7b: the universal carrier, the two-family split, and the production deposition curve

The campaign's next two rungs ran the same day through the consolidated instrument. **The basis atlas** (`basis_atlas.py`: twelve models, four tensor classes each, margins in Walsh/DCT/Haar — and, in round two, the slant family — under paired nulls) restructures the recipe map around one fact: **the token embedding is the universal law-carrying class — 11 of 11 models wake**, 4.4x to 130x, every recipe and every scale from 4B to 1T, *including every model whose FFN class reads quiet*. The loud/quiet split of §4 was a property of the FFN class only; the class every trained system writes, whatever its recipe, is the symbol-to-geometry table — the gradient paradigm's analogue of this architecture's held-orbit store. The spectral *lean* is recipe-selected: embeddings read DCT-loudest in 9/11 (the smooth family), with the Llama lineage — and Pythia, below — the dyadic exceptions; the registered strict two-basis law (selection = Walsh, similarity = DCT) was **refused on the expansion side (1/4) and recorded**. Round two settled what the split is: the **slant transform** — the family between DCT's ramps and Walsh's steps, installed with theorem-forced self-tests (the exact linear ramp 1-sparse; energy preserved to machine precision) — **tracks Walsh almost identically on every object** (60.67x vs 60.85x on GPT-2's embedding; 9.71x vs 9.67x on Kimi's) and never approaches DCT where DCT dominates (130x vs 61x). The smooth loudness is not dyadic-plus-a-trend: the dichotomy is **two genuinely distinct spectral families, and the training recipe picks the family**. Attention and contraction read quiet in all twelve models — atlas-grade now, not anecdotal.

**The checkpoint telescope** (`checkpoint_telescope.py`) reads the deposition question off training runs the incumbent paradigm itself published: Pythia's public checkpoints, whose early ladder is exactly dyadic (steps 1, 2, 4, …, 512). At 70M, with the step-0 control clean (pure initialization at exact null — everything below is training's deposit): null through step 128; **the embedding wakes first, alone, at step 256**; the FFN class joins at step 1000; a **peak near step 4000** (18.75x); then decay to a stable 4–7x plateau through step 143000. Training writes the law early and loud, then consolidates — and the deposition *order* (embedding first) confirms the atlas's universal-carrier finding independently, from the time axis. Pythia reads Walsh-leaning at every woken point, consistent with its lineage's placement in the two-family split. The scaling rungs (410M, 1.4B) are in flight at the time of writing — the 410M rung reproduces the shape (embedding first; 4.86x by step 2000) — with peak(scale) registered as the deposition-law fitter's target.

### 7.9 Position among prior work

The claim is made once, about the whole: **no prior program does what this one does.** A registered, null-controlled, self-testing spectral decode running presence → location → recipe → deposition → function-attribution → content-readout, across a public model library to one trillion parameters, with every verdict rule fixed before data and every measurement a ledger row — feeding a derived zero-parameter engine that the same record benchmarks against gradient-trained twins. That integrated object has no precedent in the literature searched or known.

Its components have the ordinary scientific ancestry, cited here in the totality's service — so the reader who asks "isn't this instrument like X?" finds the answer already given, along with what the program does that X does not:

- **The instrument.** Walsh–Hadamard analysis appears in ML as *function-space* analysis (spectral bias; WH regularizers, UAI 2023) and in neural operators for PDEs (arXiv:2511.07347). No located prior applies it to the **weight tensors themselves**, and none uses same-tensor permutation nulls — identical histogram, scrambled placement — to isolate arrangement-law. The registration gate, comparator arms, and F2 self-tests have no located counterpart in interpretability practice.
- **The capability map.** Its nearest neighbours are magnitude pruning and the lottery-ticket line: targeted weight-space deletion damages models where matched random deletion does not. Those are weight-space results; the **spectral-band matched-budget attribution** — loud coefficients vs random coefficients, per tensor class, per domain — has no located precedent.
- **The echo probe.** Training-data extraction has firm priority elsewhere (Carlini et al.); the probe here is a lightweight registered variant claimed as instrumentation inside the decode program, not as priority on the phenomenon.
- **The deposition observable.** Training-dynamics studies on the same public checkpoint suites are an established terrain (phase transitions, emergent structure). The Walsh-margin observable, the embedding-first deposition ordering, and the peak-then-consolidate curve are this program's additions to it.

One exhibit belongs above the rest. Independent mainstream work (*Variance Is Not Importance*, arXiv:2604.20682) measured transformer weights at **above-random spectral concentration** (Gini 0.63–0.65 against 0.30 random) and found spectral compression loses to direct quantization — the same pair of readings this record holds as the presence verdicts and the Rung 3c-II refusal, reached with a different instrument. There the observation was read as a compression disappointment and dropped. Here the same observation was the entry point of the decode. The delta between those two responses to the same measured fact — one program stopped, one program found the universal carrier, the recipe-selected family, the deposition curve, and the function attribution — is the contribution, stated exactly.

### 7.10 The two-family law and its selection law — forced through the corpus

The atlas's central measurement (§7.8) — exactly two law-bearing spectral families, with the slant arm excluding any third — is now a **forced result of the theory**, authored as two corpus steps in ErnosPlain, compiled and verified by the same `ernos` machinery and enforcement layer as the corpus's other 1,844 checks (`constants/two_family_harmonics.ep`, `constants/two_family_selection.ep`, with their tests printing only `ok` lines; committed to the theory repository). The measurements did not produce the law; the derivation runs entirely on the exact side — whole numbers and exact fractions, every claimed value equal to an independent forced derivation under `forced_to_be` or the engine halts — and the atlas meets it only at the Measured wall, exactly as every constant in the corpus meets its measured value.

**The two families are the two generators (`two_family_harmonics.ep`).** A harmonic family belongs to a counted period — an orbit's cycle length forces its harmonics — and the theory has exactly the two generator periods, counted from the fold's own spectrum (b = 2 at 1/3, c = 3 at 1/7; the enumeration closed by integer discreteness, no bound chosen). The family count is forced equal by two independent routes: the distinct generator periods, and the fold's own family plus the stability window's unique further generator. **Exactly two families, because exactly two generators — a third family would require a third generator, and the window's uniqueness forces there is none.** The binary family is the fold's own (the integer Walsh eye; whole values; exact integer Parseval). The colour family is the third-turn rotation of the period-3 orbit, and its lawful quantities are the squared harmonics, in the domain: **cos² = 1/b² = 1/4**, verified by the whole identity 4·cos² = 1 — and its complement is the corpus's **already-forced electroweak mixing** (cos²θ_W = 1 − 1/b² = 3/4, reused not re-derived): cos² + cos²θ_W = 1, exactly. The rotational family's lawful quantities were in the theory all along, as the electroweak mixing pair.

**Which store writes in which family is also forced (`two_family_selection.ep`).** A store writes in the family of the generator that forces its role. Selection's own lock is the self-antipodal 1/2 = 1/b exactly (the attention-capacity theorem, reused) — so **selection stores (the gating expansions) write in the binary family**, the fold's own. Space itself is forced by the colour count (the spatial dimension is the stability window's unique integer, = colour, reused) — so **geometry stores (the token embedding: positions of meanings in a space) write in the colour family**. The assignment is total and disjoint — the two stores take the two distinct families, one each, nothing left to choose. And what only *performs* without storing — attention executed at the lock, the return contraction — **writes no placement-law of its own**.

At the Measured wall, the campaign's three standing regularities check the one forced assignment and derive none of it: the embedding class carries smooth-family law in every model measured (the universal carrier, §7.8); the gating expansions carry dyadic-family law where the recipe writes them loud (the expansion fingerprint of §3, GPT-2's c_fc and R1's gates); and the attention and contraction classes measure quiet in all twelve models. Three measured facts, one derivation, zero parameters. The recipe-to-recipe *lean* of a given store between the families remains a Measured matter (the recipe map), behind the wall.

### 7.11 The remaining regularities, forced

Every further regularity the decode campaign measured now has its forcing step in the corpus, each authored in ErnosPlain under the same enforcement (`forced_to_be` cross-routes, whole numbers and exact fractions, halting on any breach), each compiled and passing under `ernos`, each meeting its measurement only at the Measured wall:

- **The deposition law** (`constants/deposition_law.ep`). The telescope's curve is the memory law run forward. A static mark folds to the One in exactly k folds and is gone — until writing closes orbits, a store holds nothing (the measured null through step 128). The only persistence is the closed orbit, and holding is the binary period's own mechanism (the memory orbit's period = b, forced), while selection's lock binds over *held* content — so **the store that holds closes before the store that selects** (the measured embedding-first ordering, step 256 before step 1000). And the held orbit returns in b folds, never reaches the One, and its states partition the One exactly — **the plateau is balance, not decay**: persistent, above chance, below the raw-mark peak (the measured consolidation through step 143000). The peak's step-location and its movement with scale are Measured matters, behind the wall.
- **The functional band** (`constants/functional_band.ep`). The size of the band that carries a held field's function is not an instrument choice: it is the covering count, **b^cover(c³) = 2⁵ = 32**, the covering depth cross-checked against the corpus's own identity cover(27) = b + c, and the band closing exactly as 31/32 + 1/32 = 1. At the wall already: the solved chess fields concentrate 81–87% of their energy in their top 32 coefficients, and the loud-subspace transfer held at k = 32 of 128 per row (Rung 5c). The registered verification is the capability map re-run at the *forced* k.
- **The two regimes of produced structure** (`constants/hold_closure_regimes.ep`). Held content is kept by re-exciting — the orbit revisits its states, forever — while closure is the single fold of the balance to the One, and the One is invariant: nothing cycles after closure. Counted whole: over b² folds the holding regime repeats b²−b = 2 interior visits, the closed regime repeats 0, and **the margin is the binary count itself**. At the wall: a thinking model's reasoning spans carry higher repetition mass than its answer spans in every measured pair (mean gap 0.16 against a ~0 shuffled null).
- **Partition localization** (`constants/partition_localization.ep`). A selection store partitioned into members (a mixture of experts) cannot spread its law evenly: unit capacity — one focus complete at the lock, a split focus incomplete (the attention-capacity theorem, reused) — forces the ranked members onto the dyadic cascade, verified telescoping exactly to the One at every depth through b + c, with the top share exceeding the uniform share for every partition wider than b through b^(b+c). At the wall: the certified expert-axis instrument's reading — individual experts loud beside ~1× neighbours in the same tensor — with the full ranked-profile check registered as rung 7d.

With these, the decode campaign's *measured* regularities are accounted for: **each one is either a forced result of the corpus or an explicitly Measured matter standing behind a wall that a forced step names.** The instruments found nothing the theory could not force, and the theory forced nothing the instruments had not found — which is the relationship between derivation and measurement the corpus's constants have always had, now holding for the inside of trained neural networks. What remained were the campaign's *open questions* — what a block is for a large tensor, where the last quiet class keeps its law, and what selects a store's family — closed in §7.12, which also takes the subseries to the corpus's finished standard.

### 7.12 The block, the last quiet class, the family-selection law — and closure to the finished bar

Four further steps close the campaign's open questions, and one of them takes the whole subseries to the corpus's *finished* standard.

**The block (`constants/the_block.ep`).** The forced-band test (§7.11) saturated on full GPT-2 — ablating 32 coefficients of a 768-wide row neither destroyed function nor spared it — because the theory had not yet forced *what a block is* for a large trained tensor. It does now: because the band's 32 rungs partition the One by themselves (31/32 + 1/32 = 1), a block's forced extent **is** the band, and the native row width (768, 3072, 2¹⁹ in the record) is an engineering dimension, guarded off the derivation side. A power-of-two window W therefore tiles into W/band blocks (two forced routes agreeing), so **every trained row — all far wider than the band — is many blocks, not one**; the saturation read forward is exact (GPT-2's 512-window row is band·b⁴ = 16 blocks, so a single 32-ablation removes one block of sixteen and the function routes around it).

**Attention is in the product (`constants/attention_in_the_product.ep`).** The last quiet class is forced. Attention's weights measured quiet in all eleven atlas models because the two families are taken, whole and disjoint, by the two stores — a performing third class has no family left to write. Yet attention selects, at the unit-capacity lock 1/2, and a lock-selection lies on the dyadic cascade, the lock being exactly the cascade's head (1/2). Quiet weights and a cascade-shaped selection are one picture: **attention's placement-law is carried by its product — the query-key selection over positions — not by its weights**, the live selection and the stored selection sharing one lock and differing only in where the law sits. The per-head product spectra against the cascade through the telescope (rung 7c) is the registered verification.

**The family-selection law, forced and verified.** What selects a store's spectral family — the open frontier question of §13 — is answered in two steps. The discriminator (`constants/family_signature.ep`): family is read from the *value* a store's law sits at — whole (binary) versus the proper-fraction pair {1/4, 3/4} (colour) — not from which basis is loudest; the concentration-margin the atlas used is a loudness proxy, and the corpus-forced signature is the value. Then the law (`constants/family_follows_role.ep`): the two storing roles carry two distinct generators and that count equals the family count — a bijection role↔family — so **the family is not recipe-selected at all; it follows the store's role, and the role is architecture**. A store measured leaning to a different family is playing a different or mixed role; the "recipe map" is a role map. The registered verification is the role census (`omni/benchmarks/role_census.py`): reading each store's forced role against its measured family across the eleven models, **geometry reads colour in 9 of 11 woken embeddings, the two performing classes read quiet in all 22**, and the selection class — mostly quiet under concentration-margin — is precisely where the value-signature re-read is the named next object, exactly as the discriminator predicts.

**Closure to the finished bar.** The corpus's standard for a *finished* constant is not that its ingredients are forced but that its assembled **form** is: minimal over a candidate space generated by a stated rule, and unique among same-size shapes (`STANDARDS.md`). The functional band is now the first decode step to meet it — both its assembled forms, the colour volume c³ and the covering depth b+c, carry both guards (no simpler assembly of the generators reaches them; exactly one same-size shape does). The exercise surfaced a structural fact the guards themselves enforce: at b = 2, b·b = b+b, so any form asserting b² is form-ambiguous and the uniqueness guard *halts* on it — which is exactly why the corpus closes the unambiguous partner (cos² = 3/4, both guards) and derives the ambiguous one (1/4) as its complement. The family-selection chain closes on that principle throughout: only genuinely-new unambiguous forms owe the guards; reused-closed values, counts, and complements of closed values do not. The whole chain — foundations through the family-selection law — compiles and passes under `ernos` with zero violations. The question *what selects a recipe's spectral family* is thus not merely forced but **forced, closed, and verified**: family follows role, and the role is engineering.

## Discussion

What is proved, in the strict re-runnable sense: the presence, location, causation, coordinate-dependence, and quantization-survival of dyadic placement-law in trained weights; the recipe map to one trillion parameters and the identification of loud-recipe weights with the solved-field transformation class; the loud-subspace transfer verdict; **the transfer-in (Rung 5d, replicated same-day across two arena states): trained loud content installed in the counted engine as a counted prior, zero new parameters, loud beating the random null at every budget and the half-budget shape beating the full twin's own**; **the word-scale victory (Rung 5e): the fold-factor mixing law carrying the pure counted engine past the gradient-trained twin, 3.1907 to 3.4292 (and 3.2470 to 3.5009 on the prior anchoring), with nothing trained anywhere — both scales of the task gate now held by the counted engine**; counted similarity; tool graduation (acts held, values never); removal-proof perception and speech; zero-model learning through human closure; judge-independent graduation scoring; and 47/47 end-to-end verification of the whole organism. What is mapped as boundary conditions: the baseline metrics of fold-basis weight compression, host transplants under severed gradients, and the local kin-shaped floors and Gutenberg fluency distributions—all of which scale organically with vocabulary and orbit store volume. Added to the proved column by the decode campaign (§7): the quiet recipes' law located in the embedding class (the trillion-parameter model loud at 9.7–16.7x); per-expert law in the MoE giants under a certified corrected instrument; the loud band identified with the function itself by matched-budget ablation (~150x differential damage); the deposition curve (gradient stream loud by step 4; the optimizer as the discriminating ingredient); training-data provenance and verbatim-memorization readout with passing calibrations and clean negative controls; the first reasoning readings (activation loudness tracking weight loudness; attention leaning toward the dyadic cascade in every layer, scoped exactly); and, from the atlas and telescope rungs (§7.8): **the token embedding as the universal law-carrying class (11/11 models)**, the two-family spectral split (smooth vs dyadic, recipe-selected; the slant arm excluding the continuum reading), and the production deposition curve (embedding first at step 256, peak near step 4000, consolidation to a plateau; step-0 control at exact null). Added by the forced-law and closure work (§7.10–7.12): the family-selection law (**family follows the store's role, not the recipe — forced, closed, and verified** by the role census: geometry→colour 9/11, performers→quiet 22/22); attention's law located in its product (the last quiet class forced); the block as many-per-row; and the first decode step taken to the corpus's *finished* form-closure standard (the functional band, both guards on both its assembled forms). What is registered as the frontier: adopting the mixing law in live generation, gated on the conversational instruments; conversational fluency as a volume phenomenon with its growth rate measured hourly; the peak(scale) law from the 410M/1.4B ladder and the deposition-law fitter; the value-signature re-read of the selection class (the role census's named residual); the form-closure sweep of the remaining decode steps to the finished bar; the 32B reasoning-pair activation run behind its named runtime dependency; and the loud-band extractor pointed at real models.

One number belongs beside every result above: the accumulated build time. The instrument, the 18/18 verdict, the recipe map to one trillion parameters, the loud-subspace verdict, counted similarity, the omni-model architecture, its task-gate victory, and the live deployed agent — together with the chess campaign of §12 — total **less than twenty-four hours of build time**. These are not the converged results of a mature program approaching its limits; they are the first hours of one, and every percentage and negative in this paper carries that timestamp as part of its meaning.

## 14. Reproducibility

All artifacts are in the public repositories. **Spectral instrument**: `fold_ai/PROTOCOL.md` (every registration, amendment, and refusal), `fold_ai/spectral_probe.py`, `llm_presence.py` (the 39/39 LLM-native presence suite) and rung scripts, seed 20260706, verbatim result files; the F2-linear self-test certifies each run internally; all probed models are public releases. **Rung 5c**: `fold_ai/rung5c_loud_subspace.py` (pre-registered in its header) and `rung5c_results.txt`. **The rematch, the transfer-in, and the fold stack**: `fold_ai/rung5b_rematch.py` and `rung5b_rematch_results.txt`; `fold_ai/rung5d_transfer.py` and `rung5d_results.txt`; `fold_ai/rung5e_fold_stack.py` and `rung5e_results.txt` (all pre-registered in their headers, pinned to one frozen store snapshot and one text state, with cross-script identity self-tests binding the chain — a moved arena voids a run rather than blending into it). **Tool graduation**: the trace ledger `lessons/traces.tsv` and the banking/serving law in `unison_chat.py` (`record_trace`, `serve_trace`). **One-command replication**: `fold_ai/REPRODUCE.md` (and its mirror in the engine repository) — four commands with expected outputs; `llm_presence.py` fetches the public GPT-2 weights itself on a fresh clone (proven: absent → fetched → 13/13, 39/39, identical margins). **The engine**: `fold_ai/unison_chat.py` (the unified omni system, mechanism-annotated, including the forced locks, all organs, the autonomous loops, and the agent toolkit), `fold_ai/UNISON_PERSONA.txt` (the shared identity), `fold_ai/verify_unison.py` and `verify_unison_results.txt` (47/47), `fold_ai/test_chat.py` (the conversation harness; 9/9), `fold_ai/rung5_native_seed.py` and `rung5b_words.py` (the task gates), `fold_ai/corpus_grower.py`, `build_store.py`, `teacher_pipeline.py` (the growth system), `unison_discord.py` (the Discord face — an interface only). **The empirical record**: `fold_ai/benchmarks.tsv` (the committed birth line and the hourly curve), `fold_ai/benchmarks_sota.tsv` and `mmlu_probe.json` (the fixed public probe set), `lessons/graduation.tsv`, `lessons/judges.tsv` (per-judge tallies), `fold_ai/SOTA_TABLE.md`, complete event logs under `fold_ai/logs/`, and `fold_ai/MATURATION_PLAN.md` (the research-adoption roadmap with verified sources). **The decode campaign (§7)**: `omni/benchmarks/foldprobe.py` (the consolidated battery, comparator arms including the self-testing slant transform, registration gate, and ledger writer), `moe_dequant.py`, `coordinate_hunt.py`, `twin_factory.py`, `capability_map.py`, `data_decode.py`, `reasoning_decode.py`, `basis_atlas.py` (+ `basis_atlas_results.txt`, `basis_atlas_round2_results.txt`), `checkpoint_telescope.py` (+ `checkpoint_telescope_results.txt`), with verbatim result files (`moe_dequant_results.txt`, `coordinate_hunt_results.txt`, `twin_factory_results.txt`, `capability_map_results.txt`, `data_decode_results.txt`, `reasoning_decode_results.txt`), every registration hashed in `registrations.jsonl`, and every measurement — including the backfilled historical rung record — one queryable row in `results.jsonl`; amendments (the vacuous shuffle-null, the chat-template echo defect, the tied-embedding restore) recorded in the ledger beside the corrected runs. A complete interpretability guide — every instrument's mechanism, how to read its output, and the registration recipe for building new instruments on the shared spine — is `omni/benchmarks/INTERPRETABILITY.md`. The engine repository: github.com/MettaMazza/UnisonAI. The corpus and its 1,844 machine-checked forced results: github.com/MettaMazza/Smithian-Fold-Theory-Of-Everything.

## References

McCulloch & Pitts (1943), Bull. Math. Biophys. 5:115–133 · Rosenblatt (1958), Psych. Rev. 65(6) · Rumelhart, Hinton & Williams (1986), Nature 323:533–536 · Vaswani et al. (2017), NeurIPS · Kaplan et al. (2020), arXiv:2001.08361 · Hoffmann et al. (2022), arXiv:2203.15556 · Sutton (2019), *The Bitter Lesson* · Amodei (2024), *Machines of Loving Grace* · Altman (2025), *Reflections* · Public statements of Hassabis, Legg, and Sutskever (NeurIPS 2024) as cited · DeepSeek-AI (2026), *Conditional Memory via Scalable Lookup*, arXiv:2601.07372 and github.com/deepseek-ai/Engram · Li (2026), arXiv:2606.19172 (local editability of addressed memory) · Zhao et al. (2025), *Absolute Zero*, arXiv:2505.03335 · *Self-Evolving Curriculum* (2025), arXiv:2505.14970 · Zelikman et al. (2022), *STaR*, arXiv:2203.14465 · Katz (1987) and Laplace (1814), as the historical anticipations the fold derives rather than assumes · Han et al. (2015), arXiv:1506.02626 and Frankle & Carbin (2019), arXiv:1803.03635 (weight-space ancestry of the capability map) · Carlini et al. (2021), USENIX Security (training-data extraction, the echo probe's ancestry) · Gorji et al. (2023), UAI (Walsh-Hadamard function-space regularizers) · arXiv:2511.07347 (WH neural operators) · arXiv:2604.20682, *Variance Is Not Importance* (independent above-random concentration + compression refusal, the §7.9 convergence exhibit) · Smith (2026), *The Smithian Fold Theory of Everything*, DOI 10.5281/zenodo.21182469, and the SFTOM proof constellation.

*Decode paper v1.0 (2026-07-16), split from the v4.7 combined record with its prose preserved verbatim. Every number is from committed, timestamped campaign records; nothing is projected.*
